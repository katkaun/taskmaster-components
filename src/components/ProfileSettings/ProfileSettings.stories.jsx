import React from "react";
import ProfileSettings from "./ProfileSettings";

export default {
  title: "taskmaster/components/ProfileSettings",
  component: ProfileSettings,
};

export const Default = () => <ProfileSettings />;